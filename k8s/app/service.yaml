apiVersion: v1
kind: Service
metadata:
  name: langconnect
  namespace: langconnect
spec:
  selector:
    app: langconnect
  ports:
    - port: 80
      targetPort: 8000
      name: http
    - port: 8080
      targetPort: 8080
      name: reverb
