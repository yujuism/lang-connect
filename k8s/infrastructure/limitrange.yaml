apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: langconnect
spec:
  limits:
    - default:
        memory: "512Mi"
        cpu: "500m"
      defaultRequest:
        memory: "64Mi"
        cpu: "50m"
      type: Container
